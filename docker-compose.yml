name: vm-runner
services:
  be:
    container_name: listener
    platform: linux/amd64
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      - RUST_LOG=debug
      - VM_ID=1
      - ORCHESTRATOR_URL=http://host.docker.internal:8080
      - LOGS_PORT=8081
      - VM_REPORT_INTERVAL=5
    ports:
      - "7000:80"
